<!doctype html>
<html lang="he" dir="rtl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>התחברות</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    /* מיקוד רק לעמודי טפסים */
    .auth-page .page{
      min-height:100svh; display:flex; align-items:center; justify-content:center; padding:24px;
    }
    .auth-page .card{ width:100%; max-width:420px; }
    .auth-page form > div{ margin-bottom:10px; }
    .auth-page input[type="email"], .auth-page input[type="password"], .auth-page input[type="tel"], .auth-page input[type="text"]{
      width:100%; box-sizing:border-box;
    }
    .auth-page .actions{ display:flex; gap:8px; flex-wrap:wrap; }
  </style>
</head>
<body class="auth-page">
  <div class="page">
    <div class="card">
      <h1>התחברות</h1>

      <% if (typeof error !== 'undefined' && error) { %>
        <p class="error"><%= error %></p>
      <% } %>

      <form method="post" action="/login" autocomplete="on">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">

        <div>
          <label>אימייל</label><br>
          <input name="email" type="email" required>
        </div>

        <div>
          <label>סיסמה</label><br>
          <input name="password" type="password" required>
        </div>

        <div class="actions" style="margin-top:10px">
          <button class="badge" type="submit">התחבר</button>
          <a class="badge" href="/forgot">שכחת סיסמה?</a>
          <a class="badge" href="/register">משתמש חדש</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
