<!doctype html>
<html lang="he" dir="rtl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>דשבורד</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body data-role="<%= user?.role || 'user' %>">
  <div class="card">
    <div class="topbar">
      <h1>שלום <%= (user?.first_name || "") + " " + (user?.last_name || "") %></h1>
      <form method="post" action="/logout">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button type="submit">התנתקות</button>
      </form>
    </div>
    <p class="muted">
      כל שורה = שעה מימין + 4 משבצות משמאל. ניתן להירשם למשבצת אחת. (אדמין: פתיחה/סגירה/ניקוי/עריכת שם)
    </p>

    <div id="grid" class="grid">

      <%
        // קיבוץ לפי שעה
        const grouped = {};
        slots.forEach(s => {
          const key = s.time_label;
          if (!grouped[key]) grouped[key] = [];
          grouped[key].push(s);
        });

        // מיון שעות כרונולוגי (HH:mm)
        const hours = Object.keys(grouped).sort((a,b)=>{
          const [ah,am]=a.split(':').map(Number); const [bh,bm]=b.split(':').map(Number);
          return (ah*60+am)-(bh*60+bm);
        });
      %>

      <% hours.forEach(time => { 
           // ממיינים את המשבצות של אותה שעה לפי col_index ומרפדים ל-4
           const cellsSorted = grouped[time]
             .slice()
             .sort((a,b)=>(a.col_index||0)-(b.col_index||0));
           const padded = [1,2,3,4].map(ci => cellsSorted.find(x => x.col_index === ci) || {
             slot_id: null, label: "", color: "#e5e7eb", time_label: time, col_index: ci, row_index: 0,
             active: false, user_id: null, first_name: null, last_name: null
           });
      %>
        <!-- תא שעה (מימין) -->
        <div class="cell time"><%= time %></div>

        <!-- 4 משבצות משמאל -->
        <% padded.forEach(s => {
             const isPlaceholder = !s.slot_id;         // אין תא במסד – מציגים פלייסהולדר נעול
             const taken = !!s.user_id;
             const mine = s.user_id && user && s.user_id === user.id;
             const showName = taken || (user && user.role==='admin' && s.label);
             const bg = taken ? '#86efac' : (s.color || '#e5e7eb');
             const disabledForUser = isPlaceholder || (!s.active || (taken && !mine));
        %>
          <div class="cell <%= mine ? 'mine' : '' %> <%= disabledForUser ? 'disabled' : '' %>"
               style="background:<%= bg %>"
               <% if (!isPlaceholder) { %>
                 data-slot-id="<%= s.slot_id %>"
                 data-taken="<%= taken ? '1' : '0' %>"
                 data-mine="<%= mine ? '1' : '0' %>"
                 data-active="<%= s.active ? '1' : '0' %>"
               <% } %>>
            <div class="slot-text"><%= showName ? (s.label || '') : '' %></div>
            <div class="badge">
              <% if (isPlaceholder) { %>לא זמין
              <% } else if (mine) { %>שלך (הסר)
              <% } else if (!s.active) { %>סגור
              <% } else if (taken) { %>תפוס
              <% } else { %>פנוי<% } %>
            </div>

            <% if (!isPlaceholder && user && user.role==='admin') { %>
              <div class="admin-actions">
                <span class="badge" data-action="clear">נקה</span>
                <% if (s.active) { %>
                  <span class="badge danger" data-action="close">סגור</span>
                <% } else { %>
                  <span class="badge success" data-action="open">פתח</span>
                <% } %>
                <span class="badge info" data-action="label">ערוך שם</span>
              </div>
            <% } %>
          </div>
        <% }) %>
      <% }) %>

    </div>
  </div>

  <script>window.CSRF_TOKEN = "<%= csrfToken %>"</script>
  <script src="/dashboard.js"></script>
</body>
</html>
