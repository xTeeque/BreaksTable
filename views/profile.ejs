<!doctype html>
<html lang="he" dir="rtl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>פרופיל</title>
  <link rel="stylesheet" href="/style.css">
  <meta name="vapid-public-key" content="<%= vapidPublicKey %>">
  <style>
    body.auth-luxe{
      background:
        radial-gradient(1200px 600px at 100% -10%, #f3f4f6 20%, transparent 70%),
        radial-gradient(900px 500px at -10% 100%, #eef2ff 10%, transparent 60%),
        #ffffff;
    }
    .auth-luxe .page{ min-height:100svh; display:flex; flex-direction:column; gap:16px; padding:24px; }
    .auth-luxe .center-wrap{ flex:1; display:flex; align-items:flex-start; justify-content:center; }
    .auth-luxe .card.luxe{
      width:100%; max-width:560px;
      padding:28px 28px;
      border-radius:20px;
      border:1px solid #e5e7eb;
      box-shadow:0 10px 30px rgba(0,0,0,.08);
      background:#fff;
    }
    .auth-luxe .actions{ display:flex; gap:8px; flex-wrap:wrap; margin-top:10px; }
    .muted { color:#6b7280; }
  </style>
</head>
<body class="auth-luxe">
  <div class="page">
    <div class="card topbar">
      <div class="right">
        <span>שלום, <strong><%= [user?.first_name || "", user?.last_name || ""].join(" ").trim() %></strong></span>
      </div>
      <div class="left">
        <a class="badge" href="/">חזרה לדשבורד</a>
        <form method="post" action="/logout" class="inline" style="margin-inline-start:8px">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <button type="submit" class="badge">התנתקות</button>
        </form>
      </div>
    </div>

    <div class="center-wrap">
      <div class="card luxe">
        <h2>התראות בדפדפן</h2>
        <p class="muted">קבל/י תזכורת 3 דקות לפני המשבצת שלך — גם כשהאתר סגור.</p>

        <div class="actions">
          <button class="badge" type="button" data-push-enable>הפעל התראות</button>
          <button class="badge danger" type="button" data-push-disable>בטל התראות</button>
        </div>
        <p style="margin-top:8px"><strong>סטטוס:</strong> <span data-push-status>בודק...</span></p>
      </div>
    </div>
  </div>

  <script>
    window.CSRF_TOKEN = "<%= csrfToken %>";
  </script>
  <script src="/push.js" defer></script>
</body>
</html>
